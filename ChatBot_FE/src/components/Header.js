import {AppBar, Toolbar} from '@material-ui/core'

const Header = () => {
  return (
    <AppBar position='static'>
      <Toolbar>
        <h2>ChatBot App</h2>
      </Toolbar>
    </AppBar>
  )
}

export default Header
